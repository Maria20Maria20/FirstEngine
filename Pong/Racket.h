#pragma once
#include <vector>
#include "../CPP/Square.h"

class Racket:public Square
{
public:

private:
	//std::vector<Square> rackets;
	//void MoveNPCRacket(int minClampValue, int maxClampValue);
	////move npc racket = MoveShape from Square class + minClampValue for bottom border +
	////maxClampValue for up border. If racket position upper than maxClampValue, then reflection it.
	////Too if racket position below minClampValue, that is * (-1)
	//void MovePlayerRacket();
};